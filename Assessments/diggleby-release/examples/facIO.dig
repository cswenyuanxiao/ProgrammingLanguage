def fac(x: Int): Int {
  if (x == 0) {
    1
  } else {
    let minusone = x - 1 in
    x * fac(minusone)
  }
}

def getBound(): Int {
  (getInput() : Int)
}

let result = fac(getBound()) in
print((result : String))